<% @categories.each do |category| %>
<div><%= category.name %>
<%= button_to "Delete", category_path(category), method: :delete, data: { confirm: "Are you sure you want to delete this food?" } %>
</div>
<% end %>